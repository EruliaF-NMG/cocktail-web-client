const inputBoxType = {
    TEXT:"text",
    PASSWORD:"password"
}

const emptyFunction = (...para) => undefined;

const randomListLimit = 5;

export {
    inputBoxType,
    emptyFunction,
    randomListLimit
}